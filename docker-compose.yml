version: '3.8'

services:
  app:
    build: .
    restart: always
    expose:
      - "3000"
    environment:
      - PORT=3000

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
